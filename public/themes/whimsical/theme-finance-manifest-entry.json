/* ============================================================
   FINANCE
   Scoped visual theme. No global layout/cursor/companion overrides.
   ============================================================ */

body.theme-finance {
    --theme-ink: #edf6ed;
    --theme-paper: #13261d;
    --theme-accent: #3c9b68;
    --theme-accent-2: #d2b25e;
    --theme-gold: #d2b25e;

    --black: var(--theme-ink);
    --white: var(--theme-paper);
    --bg-overlay: transparent;

    --thick-border: 2px solid color-mix(in srgb, var(--theme-accent) 44%, transparent);
    --thin-border: 1px solid color-mix(in srgb, var(--theme-accent) 30%, transparent);
    --dashed-border: 1px dashed color-mix(in srgb, var(--theme-accent) 30%, transparent);
    --dotted-border: 1px dotted color-mix(in srgb, var(--theme-accent) 30%, transparent);

    --track-bg: color-mix(in srgb, var(--theme-accent) 12%, transparent);
    --muted-text: color-mix(in srgb, var(--theme-ink) 58%, transparent);

    background:
        radial-gradient(circle at 50% 4%, color-mix(in srgb, var(--theme-accent-2) 16%, transparent), transparent 34%),
        radial-gradient(circle at 12% 80%, color-mix(in srgb, var(--theme-accent) 12%, transparent), transparent 30%),
        linear-gradient(145deg, #102119, #1f3d2e) !important;

    background-attachment: fixed !important;
    color: var(--theme-ink);
}

#finance-background {
    position: fixed;
    inset: 0;
    width: 100vw;
    height: 100vh;
    overflow: hidden;
    pointer-events: none;
    user-select: none;
    z-index: 0;
}

#finance-background .theme-finance-haze {
    position: absolute;
    inset: 0;
    background:
        radial-gradient(circle at 50% 14%, color-mix(in srgb, var(--theme-paper) 42%, transparent), transparent 34%),
        radial-gradient(circle at 15% 55%, color-mix(in srgb, var(--theme-accent) 10%, transparent), transparent 30%),
        radial-gradient(circle at 85% 58%, color-mix(in srgb, var(--theme-accent-2) 10%, transparent), transparent 30%);
}

#finance-background .theme-finance-pattern {
    position: absolute;
    inset: 0;
    background: repeating-linear-gradient(0deg, rgba(82,175,120,.05) 0 1px, transparent 1px 26px);
    background-size: 80px 80px;
    opacity: .65;
}

#finance-background .theme-finance-horizon {
    position: absolute;
    left: -7vw;
    right: -7vw;
    bottom: -8vh;
    height: 29vh;
    background:
        linear-gradient(180deg, transparent, color-mix(in srgb, var(--theme-accent) 8%, transparent) 35%, color-mix(in srgb, var(--theme-ink) 12%, transparent));
    clip-path: polygon(0 35%, 100% 12%, 100% 100%, 0 100%);
    opacity: .65;
}

#finance-background .theme-finance-item {
    position: absolute;
    width: 125px;
    height: 125px;
    color: color-mix(in srgb, var(--theme-ink) 82%, var(--theme-accent));
    opacity: .30;
    transform:
        translate3d(-50%, -50%, 0)
        rotate(var(--item-rotate))
        scale(var(--item-scale));
    filter:
        drop-shadow(0 8px 13px rgba(0,0,0,.16))
        drop-shadow(0 0 9px color-mix(in srgb, var(--theme-accent) 16%, transparent));
    animation:
        themeFinanceFloat
        7.8s
        ease-in-out
        var(--item-delay)
        infinite;
    transform-origin: center center;
    will-change: transform;
    pointer-events: none;
}

#finance-background .theme-finance-item svg {
    width: 100%;
    height: 100%;
    display: block;
    overflow: visible;
    pointer-events: none;
}

#finance-background .theme-finance-item:nth-of-type(3n) {
    width: 105px;
    height: 105px;
    opacity: .24;
}

#finance-background .theme-finance-item:nth-of-type(4n) {
    animation-duration: 9.4s;
}

@keyframes themeFinanceFloat {
    0%,100% {
        transform:
            translate3d(-50%, -50%, 0)
            translate3d(0,0,0)
            rotate(var(--item-rotate))
            scale(var(--item-scale));
    }
    50% {
        transform:
            translate3d(-50%, -50%, 0)
            translate3d(var(--item-x), var(--item-y), 0)
            rotate(calc(var(--item-rotate) + 2deg))
            scale(var(--item-scale));
    }
}

#finance-background .theme-finance-react {
    opacity: .56;
    filter:
        drop-shadow(0 10px 16px rgba(0,0,0,.20))
        drop-shadow(0 0 14px color-mix(in srgb, var(--theme-accent) 34%, transparent));
    animation:
        themeFinanceReact
        .5s
        ease-out !important;
}

@keyframes themeFinanceReact {
    0% {
        transform:
            translate3d(-50%, -50%, 0)
            rotate(var(--item-rotate))
            scale(var(--item-scale));
    }
    45% {
        transform:
            translate3d(-50%, -50%, 0)
            translate3d(0,-10px,0)
            rotate(calc(var(--item-rotate) + 7deg))
            scale(calc(var(--item-scale) * 1.09));
    }
    100% {
        transform:
            translate3d(-50%, -50%, 0)
            rotate(var(--item-rotate))
            scale(var(--item-scale));
    }
}

/* UI adaptation only. Sizes and positions remain template-owned. */
body.theme-finance .day-box,
body.theme-finance .day-picker-box,
body.theme-finance .icon-btn,
body.theme-finance .small-icon-btn,
body.theme-finance input,
body.theme-finance textarea,
body.theme-finance select,
body.theme-finance .notes-editable,
body.theme-finance .phrase-card,
body.theme-finance .chip,
body.theme-finance .toolbox-item,
body.theme-finance .resource-card,
body.theme-finance .polaroid-card,
body.theme-finance .filter-tab,
body.theme-finance .day-target-box,
body.theme-finance .flashcard,
body.theme-finance .flashcard-mode-card,
body.theme-finance .day-badge,
body.theme-finance .video-placeholder,
body.theme-finance .theme-picker-card,
body.theme-finance .note-audio-row,
body.theme-finance .modal-box {
    background-color: color-mix(in srgb, var(--theme-paper) 91%, transparent);
    color: var(--theme-ink);
    border-color: color-mix(in srgb, var(--theme-accent) 28%, transparent);
}

body.theme-finance input::placeholder,
body.theme-finance textarea::placeholder,
body.theme-finance .notes-editable:empty::before {
    color: color-mix(in srgb, var(--theme-ink) 43%, transparent);
}

body.theme-finance .icon-btn:hover,
body.theme-finance .small-icon-btn:hover,
body.theme-finance .filter-tab:hover {
    background: color-mix(in srgb, var(--theme-accent) 14%, var(--theme-paper));
    color: var(--theme-ink);
}

body.theme-finance .chip:hover,
body.theme-finance .phrase-card:hover,
body.theme-finance .filter-tab.active,
body.theme-finance .day-picker-box.selected {
    background:
        linear-gradient(
            135deg,
            color-mix(in srgb, var(--theme-accent) 90%, var(--theme-paper)),
            color-mix(in srgb, var(--theme-accent-2) 76%, var(--theme-paper))
        );
    color: var(--theme-paper);
    border-color: color-mix(in srgb, var(--theme-accent) 58%, transparent);
}

body.theme-finance .progress-bar-track {
    background: color-mix(in srgb, var(--theme-accent) 12%, transparent);
}

body.theme-finance .progress-bar-fill {
    background:
        linear-gradient(
            90deg,
            var(--theme-accent),
            var(--theme-gold),
            var(--theme-accent-2)
        );
}

/* Safe stacking: no broad direct-child wildcard selectors. */
body.theme-finance #finance-background {
    z-index: 0;
}

body.theme-finance .view {
    position: relative;
    z-index: 2;
}

body.theme-finance .side-nav {
    position: relative;
    z-index: 3;
}

body.theme-finance header,
body.theme-finance .log-header-container {
    position: relative;
    z-index: 4;
}

body.theme-finance .modal,
body.theme-finance .modal-overlay,
body.theme-finance .overlay {
    z-index: 10;
}

@media (max-width: 700px) {
    #finance-background .theme-finance-item {
        width: 94px;
        height: 94px;
        opacity: .21;
    }

    #finance-background .theme-finance-item:nth-of-type(2n) {
        opacity: .14;
    }
}

@media (prefers-reduced-motion: reduce) {
    #finance-background * {
        animation: none !important;
    }
}
